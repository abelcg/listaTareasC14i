import React, { Component } from 'react';

class Subtitulo extends Component {
    render() {
        return (
            <p className="text-center lead">Lista de tareas usando React y localStorage</p>
        );
    }
}

export default Subtitulo;